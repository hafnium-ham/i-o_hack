# SportsCast UI

Post-game interview transcription and stats overlay — Next.js frontend.

## Setup

```bash
bun install
bun dev
```

Open [http://localhost:3000](http://localhost:3000).

## Usage

Paste a YouTube URL into the input bar and hit **Load** to play a video. The right panel will show live transcription once the backend is wired up.
